{
  "name": "stock-alert-extension",
  "version": "1.2.1",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check:syntax": "node --check storage.js && node --check quotes.js && node --check background.js && node --check popup.js",
    "check:types": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "check": "npm run check:syntax && npm run check:types && npm run lint && npm run validate:manifest",
    "ci": "npm run check && npm run test:unit"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.2",
    "c8": "^12.0.0",
    "eslint": "^10.8.0",
    "typescript": "^7.0.2"
  }
}
