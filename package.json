{
  "name": "stock-alert-extension",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check:syntax": "node --check stock-utils.js && node --check storage.js && node --check quotes.js && node --check quote-service.js && node --check quote-format.js && node --check router.js && node --check popup-bridge.js && node --check popup-state.js && node --check popup-render.js && node --check popup-actions.js && node --check popup.js && node --check background.js",
    "check:types": "tsc --build tsconfig.json --pretty false",
    "check:architecture": "node scripts/check-architecture.mjs",
    "check:runtime-imports": "node scripts/check-runtime-imports.mjs",
    "lint": "eslint .",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "capture:store": "node scripts/capture-store-assets.mjs",
    "package:extension": "node scripts/package-extension.mjs",
    "check:bundle": "node scripts/check-bundle-size.mjs",
    "release:verify": "npm run ci && npm run capture:store && npm run package:extension && npm run check:bundle",
    "test:unit": "c8 --all --include=stock-utils.js --include=storage.js --include=quotes.js --include=quote-service.js --include=quote-format.js --include=router.js --include=popup-bridge.js --include=popup-state.js --include=background.js --check-coverage --lines 90 --branches 85 --functions 90 --statements 90 node --test tests/unit/*.test.mjs",
    "test:contracts": "node --test tests/contracts/*.test.mjs",
    "test:e2e": "playwright test",
    "check": "npm run check:types && npm run lint && npm run check:architecture && npm run build && npm run check:runtime-imports && npm run validate:manifest",
    "ci": "npm run check && npm run test:unit && npm run test:e2e",
    "build": "node scripts/build-extension.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@babel/core": "^8.0.1",
    "@babel/eslint-parser": "^8.0.1",
    "@babel/preset-typescript": "^8.0.1",
    "@playwright/test": "^1.62.1",
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.2",
    "c8": "^12.0.0",
    "eslint": "^10.8.0",
    "fflate": "^0.8.3",
    "happy-dom": "^20.11.1",
    "tsx": "^4.23.5",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.65.0"
  }
}
